config:
  target: http://localhost:3003/api
  phases:
    - duration: 60
      arrivalRate: 5
      name: Warm up
    - duration: 60
      arrivalRate: 5
      rampTo: 500
      name: Ramp up load
    - duration: 60
      arrivalRate: 100
      name: Sustained load

scenarios:
  - name: "Search and buy"
    flow:
      - get:
          url: "/reviews/168"
      # - get:
      #     url: "/ratings/168"